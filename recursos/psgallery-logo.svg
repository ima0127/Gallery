
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 600 120" class="psgallery-logo">
  <style>
    .neon-text {
      fill: none;
      stroke: var(--neon-color, #00ffff);
      stroke-width: 1.5;
      font-family: 'Arial', sans-serif;
      font-size: 60px;
      dominant-baseline: middle;
      text-anchor: middle;
      paint-order: stroke fill;
      filter: drop-shadow(0 0 6px var(--neon-color, #00ffff)) drop-shadow(0 0 10px var(--neon-color, #00ffff));
    }
  </style>
  <rect width="100%" height="100%" fill="var(--bg-color, #0d0d0d)" />
  <text x="50%" y="50%" class="neon-text">PSGallery</text>
</svg>
